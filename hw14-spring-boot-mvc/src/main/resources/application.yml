server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5430/hw14
    username: usr
    password: pwd
  flyway:
    enabled: true
    validateMigrationNaming: true

logging:
  level:
    org.springframework.jdbc.core.JdbcTemplate: TRACE
